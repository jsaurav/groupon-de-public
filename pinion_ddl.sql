CREATE TABLE `<schema_name>`.pinion` (
  `PIPELINE_NAME` varchar(500) NOT NULL,
  `PINION_API_NAME` varchar(20) DEFAULT NULL,
  `HIVE_SCHEMA_NAME` varchar(100) DEFAULT NULL,
  `HIVE_TABLE_NAME` varchar(100) DEFAULT NULL,
  `SNF_SCHEMA_NAME` varchar(256) DEFAULT NULL,
  `SNF_TABLE_NAME` varchar(256) DEFAULT NULL,
  `S3_TARGET_PATH` varchar(1000) DEFAULT NULL,
  `IS_COMPACTION_ENABLED` boolean DEFAULT true,
  `COMPACTION_FREQUENCY` varchar(256),
  `TABLE_SIZE` decimal(10,2) DEFAULT NULL,
  `TOTAL_OBJECTS` bigint(20),
  `NUM_OF_PARTITIONS` bigint(20) DEFAULT NULL,
  `FILES_THRESHOLD_FACTOR` bigint(20) DEFAULT NULL,
  `LAST_COMPACTION_RUN_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `NEXT_COMPACTION_RUN_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `IS_FORCE_COMPACTION_ENABLED` boolean DEFAULT false,
  `IS_DELTA_TABLE` boolean DEFAULT true,
  `IS_PARTITIONED_HIVE_TABLE` boolean DEFAULT false,
  `IS_PARTITIONED_SNF_TABLE` boolean DEFAULT false,
  `OWNER` varchar(256) DEFAULT NULL,
  `YAML_FILE_PATH` varchar(1000) DEFAULT NULL,
  `IS_SIZE_FETCH_ENABLED` boolean DEFAULT true,
  `SIZE_FETCH_FREQUENCY` varchar(256),
  `LAST_FETCH_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `NEXT_FETCH_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `INSERTION_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `INSERTION_USER` varchar(256) DEFAULT NULL,
  `LAST_UPDATED_DATE` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `LAST_UPDATED_USER` varchar(256) DEFAULT NULL,
  UNIQUE KEY `id` (`PIPELINE_NAME`, `PINION_API_NAME`,`HIVE_SCHEMA_NAME`,`HIVE_TABLE_NAME`)
) ENGINE=MyISAM AUTO_INCREMENT=1 DEFAULT CHARSET=latin1;
